# Server Configuration
PORT=3000
NODE_ENV=development

# Session Secret (REQUIRED - MUST BE CHANGED!)
# Generate a secure secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# DO NOT use the default value below - the server will not start
SESSION_SECRET=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY

# Database (SQLite file will be created automatically)
# No configuration needed for SQLite

# Base URL Configuration
# IMPORTANT: This URL is used in email verification links and other emails
# Development: Use http://localhost:3000
# Production: Use your FQDN with https (e.g., https://yourdomain.com)
BASE_URL=http://localhost:3000

# Email Configuration - Option 1: Custom SMTP Server
# Use this if you have your own SMTP server (e.g., HostGator, cPanel, etc.)
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_TLS_REJECT_UNAUTHORIZED=true
EMAIL_FROM=AI Prompt Templates <noreply@yourdomain.com>

# SMTP Port Configuration Guide:
# - Port 587 with SMTP_SECURE=false (recommended for most servers - uses STARTTLS)
# - Port 465 with SMTP_SECURE=true (uses SSL/TLS)
# - Port 25 with SMTP_SECURE=false (legacy, often blocked)

# Email Configuration - Option 2: Third-party Email Service
# Use this for services like Gmail, SendGrid, etc.
# Comment out SMTP_HOST and SMTP_PORT if using this option
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
# EMAIL_FROM=AI Prompt Templates <your-email@gmail.com>

# Security Notes:
# 1. Always generate a strong random SESSION_SECRET
# 2. Set NODE_ENV=production when deploying
# 3. Use HTTPS in production (secure cookies enabled automatically)
# 4. Never commit the .env file to version control
